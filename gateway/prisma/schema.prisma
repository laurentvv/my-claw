// Prisma schema — my-claw
// Sera complété en module 2
// Pour l'instant juste le datasource

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

// Models ajoutés en MODULE 2
// - Conversation
// - Message
// - CronJob
// - Settings
